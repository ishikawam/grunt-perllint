var grunt = require('grunt');

exports.copy = {

    main: function(test) {
        'use strict';

        test.expect(1);

        // not yet
        test.equal(true, true);

        test.done();
    },
};
